machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - make streammachine
    - make streammachine-master
    - make streammachine-slave
test:
  override:
    - docker-compose up -d
